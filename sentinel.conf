# specify path to gravium.conf or leave blank
# default is the same as GraviumCore
#gravium_conf=/home/evan82/.graviumcore/gravium.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
