# specify path to hilux.conf or leave blank
# default is the same as HiluxCore
#hilux_conf=/home/evan82/.hiluxcore/hilux.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
